{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the settings and widget windows",
  "windows": ["settings", "widget"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "store:default",
    "notification:default"
  ]
}
