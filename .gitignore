# macOS
.DS_Store

# Node / frontend
node_modules/
dist/
dist-ssr/
*.local
.vite/
.eslintcache

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Claude Code — per-user local settings (machine paths + permission allowlist).
# Never commit: the shared .claude/settings.json is fine, the *.local.json is not.
.claude/settings.local.json
# Ephemeral per-worktree session state written by concurrent Claude Code sessions.
.claude/worktrees/

# Internal scaffolding (design briefs, build prompts, planning docs) — kept in the
# tree for contributors locally but not published as user-facing docs.
docs/internal/

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Build output
*.app
*.dmg
